export interface JournalSnapshot { readonly id: string; readonly version: number; readonly payload: string; }
export interface BudgetSnapshot { readonly id: string; readonly version: number; readonly payload: string; }
export interface AccountSnapshot { readonly id: string; readonly version: number; readonly payload: string; }
export interface GoalSnapshot { readonly id: string; readonly version: number; readonly payload: string; }
